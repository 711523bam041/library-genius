# Core Flask
flask==3.0.0
gunicorn==21.2.0
flask-cors==4.0.0
werkzeug==3.0.1

# Database
mysql-connector-python==8.3.0

# Utilities
python-dotenv==1.0.0
requests==2.31.0

# AI Integration
openai==1.12.0

# Image Processing (optional)
pillow==10.2.0

# Removed packages that cause build failures:
# - opencv-python (requires system dependencies)
# - pytesseract (requires tesseract-ocr)
# - pyzbar (requires libzbar)
# - numpy (large, not essential)
